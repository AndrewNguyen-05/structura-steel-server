server:
  port: 8010
  servlet:
    context-path: /core-service

spring:
  application:
    name: core-service
  profiles:
    active: dev
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${random.value}
        health-check-path: ${server.servlet.context-path}/actuator/health
        health-check-interval: 30s
        fail-fast: false
        prefer-ip-address: true
        query-passing: true
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/core_service
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  sql:
    init:
      mode: never
  task:
    scheduling:
      pool:
        size: 5

management:
  endpoints:
    web:
      exposure:
        include: "*"
  #  zipkin:
  #    tracing:
  #      endpoint: http://${ZIPKIN_HOST}:${ZIPKIN_PORT}/api/v2/spans
  tracing:
    sampling:
      probability: 1.0

#keycloak:
#  url: ${KC_URI}
#  realm:
#    master: ${KC_REALM_MASTER}
#    hometopia: ${KC_REALM_HOMETOPIA}
#  client:
#    admin-cli: ${KC_ADMIN_CLI_CLIENT}
#    hometopia: ${KC_HOMETOPIA_CLIENT}
#  admin:
#    username: ${KC_ADMIN_USERNAME}
#    password: ${KC_ADMIN_PASSWORD}
